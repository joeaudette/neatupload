Pass total async size like we pass num async files.  Use that to make progress bar show total progress
instead of per-file progress.
Allow files to be deleted from queue.
Non-flash fallback for MultiFile.
Allow per-file progress to be displayed when Flash is being used.
Allow some HTML (e.g. a help link) to be displayed only when Flash is being used. 

Hide/stop progress bar when an error occurs (eg upload too large).  Requires using javascript to 
add postBackID to the action query string.
Target progress bar to a frame
Multi-file
Zip files
destDir, destName, and overwrite properties (stored in viewstate)

Split parser into separate class.
Comment/document code


Document design (eg write an article about it)
Acceptance tests
